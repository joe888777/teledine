import { lightTheme } from '@rainbow-me/rainbowkit';

const customTheme = lightTheme({
  accentColor: '#FF6B35', // 自定义的主要颜色
  accentColorForeground: 'white',
  borderRadius: 'small',
  fontStack: 'system',
});

export default customTheme;
